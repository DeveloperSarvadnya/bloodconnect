# Copy to all.yml (or better: encrypt with ansible-vault) and fill in real values.
# NEVER commit the filled-in version — it's gitignored.
dockerhub_username: "yourdockerhubuser"
dockerhub_password: "your-dockerhub-token"
supabase_url: "https://your-project.supabase.co"
supabase_anon_key: "your-anon-key"
