all:
  children:
    app_servers:
      hosts:
        # Replace with the Elastic IP output by 'terraform output app_server_public_ip'
        # after provisioning the staging environment.
        staging-app-01:
          ansible_host: REPLACE_WITH_TERRAFORM_OUTPUT_IP
          ansible_user: ubuntu
          ansible_ssh_private_key_file: ~/.ssh/bloodconnect-staging-key.pem
      vars:
        environment_name: staging
