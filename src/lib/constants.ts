/**
 * Default radius (km) used everywhere a donor/request is matched by
 * proximity: the donor dashboard's "nearby requests" list and the
 * hospital's "matched donors" list both use this same value, so the
 * two sides of a match always agree on what "nearby" means.
 */
export const DEFAULT_MATCH_RADIUS_KM = 50;
